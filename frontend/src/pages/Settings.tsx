import React from 'react';
import { Container, Typography } from '@mui/material';

const Settings: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">Settings - To be implemented</Typography>
    </Container>
  );
};

export default Settings;