import React from 'react';
import { Container, Typography } from '@mui/material';

const Jobs: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">Jobs - To be implemented</Typography>
    </Container>
  );
};

export default Jobs;