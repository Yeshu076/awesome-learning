import React from 'react';
import { Container, Typography } from '@mui/material';

const Register: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">Register - To be implemented</Typography>
    </Container>
  );
};

export default Register;